import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:Breja/app/c/c_page.dart';

void main() {
  testWidgets('CPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(CPage(title: 'C')));
    //  final titleFinder = find.text('C');
    //  expect(titleFinder, findsOneWidget);
  });
}
