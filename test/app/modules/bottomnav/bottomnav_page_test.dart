import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:Breja/app/modules/bottomnav/bottomnav_page.dart';

void main() {
  testWidgets('BottomnavPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(BottomnavPage(title: 'Bottomnav')));
    //  final titleFinder = find.text('Bottomnav');
    //  expect(titleFinder, findsOneWidget);
  });
}
